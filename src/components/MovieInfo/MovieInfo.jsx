import React from 'react';

const MovieInfo = ({ movie }) => {
  console.log(movie);
  return (
    <div>
      {/* {movie.map((m) => (
        <p>{m}</p>
      ))} */}
    </div>
  );
};

export default MovieInfo;
